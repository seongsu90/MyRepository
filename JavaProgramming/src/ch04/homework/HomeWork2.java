package ch04.homework;

public class HomeWork2 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=1; // Y가 1일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++)//x가 1에서 10까지 순차적으로 증가한다.
				{
			int y=2; // Y가 2일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=3; // Y가 3일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=4; // Y가 4일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=5; // Y가 5일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=6; // Y가 6일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=7; // Y가 7일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=8; // Y가 8일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=9; // Y가 9일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
		
		for(int x=1;x<=10;x++) //x가 1에서 10까지 순차적으로 증가한다.
		{
			int y=10; // Y가 10일경우
			if(4*x+5*y==60) // 문제의 방정식에 X,Y가 대입되어 60이라는 조건이 TRUE 일때 조건문 실행
			{
				System.out.println("("+x+","+y+")"); // 해당결과값 출력
			}
		}
						
		/*for(int x=1;x<=10;x++) // 1<= x <= 10 를 나타내는 x의 for문
		{
			for(int y=1;y<=10;y++) // 1<=y<=10를 나타내는 y의 for문
			{
				if(4*x+5*y==60) // if조건문에서 문제의 방정식을 조건으로 두고 60의 조건에 맞을때 (x,y)를 출력
				{
					System.out.println("("+x+","+y+")"); // 조건문에 해당되는 값을 출력
				}
			}
		}*/
		
		
	}

}
